# INDEX_AND_EMBED — seed42-tiny re-index after fixture body re-bake

**Run timestamp:** 2026-04-29 02:11:23 (local)
**Data dir:** `sample/fixtures/seed42-tiny`
**Trigger:** placeholder bodies in fixture were replaced with real LLM-generated content via Phase A of the rebake chain (live push of the OpenAI body cache through Discourse + scrape round-trip). The existing graph was extracted from `<placeholder body for topic N post M>` literals, so it captures titles + structural metadata only. Re-indexing extracts entities and relations from real prose for the first time on this fixture.

## Starting config

| Setting | Value | Source |
|---|---|---|
| Provider | OpenAI | `OPENAI_API_KEY` present in project-root `.env` |
| Extraction model | `gpt-4.1-mini` | User pick (matches last skill-run JSON 2026-04-27; overrides current data-dir `.env` that was hand-edited to `qwen2.5:14b`) |
| Query model | `gpt-4.1-mini` | Inherits extraction |
| Gleaning passes | 1 | Matches last skill-run + default |
| OpenAI embedding | `text-embedding-3-small` (1536d) | Matches last skill-run; consistent with prior faiss meta |
| LLM concurrency | 8 | OpenAI default |
| Summary-skip threshold | 8 | LightRAG default |
| Vocabulary | `entity_types.json` (5-type vocab from prior `discover-entity-types` run) | Unchanged |

## User decisions

- "Match last run exactly" — single AskUserQuestion, no overrides. Rationale: keeps QUERY-GUIDE numbers comparable and avoids changing more than one variable at a time (the body-content delta is the experiment; the model/embedding/gleaning stack is a control).
- Backup taken: `graphrag.bak.20260429-021013/` (sibling of `graphrag/`, full directory copy before --clear).

## Skipped runbook steps + rationale

- **Step 3 (rate-limit probe)** — skipped. 33-topic corpus + gpt-4.1-mini default concurrency = sub-minute runtime; OpenAI Tier 1 RPM/TPM is orders of magnitude over what 33 topics needs.
- **Step 5 (50-topic validation sample)** — skipped. Corpus IS 33 topics; can't sample a smaller subset. The full run effectively *is* the validation sample.

## Cost estimate

Coarse: 33 topics × 1–4 posts/topic × ~500 chars/post ≈ 50–80 KB total content. Add 10–20% for chunk metadata. Extraction at gpt-4.1-mini pricing is roughly $0.005 per 10K tokens. Estimated full-run spend: **$0.05–$0.15**. Within the user-approved Phase B budget ($0.10–$0.50).

## Run log

- **Pass 1** (structural KG insertion): 33/33 topics seeded. Sub-second.
- **Pass 2** (LLM entity + relation extraction over real bodies): completed.
  - 11 LLM-format warnings on chunk-f2b36ca... — that's the Discourse default FAQ topic ("Civilized Place For Public Discussion"), not seeded content. LLM produced 5 fields where 4 were expected; LightRAG silently dropped the malformed entries. Two entity_type slots ended up holding chunk IDs (`chunk-17db7f1f…`, `chunk-e49542a2…`) — minor noise from the same root cause; non-fatal.
- **Pass 3** (structural type re-assertion): 78/78 entities re-asserted at concurrency=8. 0s elapsed, parallelized.
- **Pass 4** (canonicalization): 22 case-fold merges (e.g. `Crown of Brine` → `Crown Of Brine`), 0 user-paraphrase buckets. Net: 22 ent-upserts, 272 rel-upserts, 25 ent-deletes, 456 rel-deletes.

## Post-index verification

| Check | Value | Pass? |
|---|---|---|
| Topics in graph | 33 (source: 33) | ✓ |
| Categories | 8 (6 seeded + 2 Discourse defaults) | ✓ |
| Tags | 26 | ✓ |
| Users | 11 (10 seeded + admin) | ✓ |
| Doc status | 33/33 `processed`, 0 failed | ✓ |
| Total nodes | 404 | — |
| Total edges | 660 | — |

## Before/after comparison (placeholder bodies → real bodies)

| Dimension | Prior graph (placeholders) | New graph (real bodies) | Δ |
|---|---|---|---:|
| Total nodes | 216 | 404 | +87% |
| Total edges | 404 | 660 | +63% |
| `issue` entities | 25 | 118 | 4.7× |
| `guide` entities | 29 | 56 | 1.9× |
| `mod` entities | 5 | 31 | 6.2× |
| `character` entities | 14 | 22 | 1.6× |
| `game` entities | 8 | 19 | 2.4× |
| `other` (out-of-vocab) | 54 | 71 | +31% |

Real bodies extract dramatically richer content entities, especially `issue` (forum-style problem reports with cause/symptom/fix language) and `mod` (mods, builds, plugins discussed by name). Structural counts (topics/categories/tags/users) are unchanged — those are seeded deterministically.

## Final shippability judgment

Graph is shippable.

- All four post-index verifications pass.
- The 11 Pass-2 format warnings are bounded to one Discourse default topic and produced ~2 chunk-id-shaped entity_type values in the noise floor. They don't propagate to retrieval (the chunk-id strings won't match any user's filter and won't appear as top-degree nodes).
- The real-body re-extraction quadrupled `issue` entities and 6×'d `mod` entities — the headline reason for doing this rebake. The graph now reflects what the corpus actually says, not just what topic titles imply.

## Next: regenerate the query guide

**Query guide regeneration needed: YES.** The corpus scale (216 → 404 nodes, 404 → 660 edges), entity-type histogram (`issue` 25 → 118, `mod` 5 → 31, …), top-degree entity list, and edge-verb frequencies are all stale versus the prior `<data-dir>/QUERY-GUIDE.md`. The §6 question library was tailored against the old shape; leaving it stale will have `/query-advisor` referencing entities + verbs that no longer match reality.

Run `/create-query-guide sample/fixtures/seed42-tiny` to refresh (~$0.05, ~30s).

