# Stats and query commands — Seed42-tiny community-health audit

**Report:** [`../01-2026-04-29-full-corpus.md`](../01-2026-04-29-full-corpus.md)
**Showboat replayable:** [`01-2026-04-29-full-corpus-showboat.md`](01-2026-04-29-full-corpus-showboat.md)
**Generated:** 2026-04-29
**Corpus:** seed42-tiny (`sample/fixtures/seed42-tiny`), 33 topics, 10 users
**Depth:** L2 (stats + 3 × local drill-down)

Re-run any block below against a live data directory to verify a number or rebuild a probe.
All `stats sql` blocks use `--path sample/fixtures/seed42-tiny` — substitute your own data dir if needed.

---

## Schema verification

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "DESCRIBE topic_threads"
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "DESCRIBE topic_summary"
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "DESCRIBE user_activity"
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "DESCRIBE posts"
```

---

## Skeleton probes

### Probe 1 — Median first-response time per category

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "
SELECT category,
       COUNT(*) AS topics,
       ROUND(EXTRACT(EPOCH FROM MEDIAN(response_time))/3600, 1) AS hours_to_first_reply,
       ROUND(EXTRACT(EPOCH FROM MIN(response_time))/3600, 1) AS min_hours,
       ROUND(EXTRACT(EPOCH FROM MAX(response_time))/3600, 1) AS max_hours
FROM topic_threads
WHERE response_time IS NOT NULL
GROUP BY category
ORDER BY hours_to_first_reply DESC, category
"
```

### Probe 2 — Top responder per category + concentration flag

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "
WITH replies AS (
  SELECT category, username, COUNT(*) AS reply_count
  FROM posts
  WHERE post_number > 1
  GROUP BY category, username
),
totals AS (
  SELECT category, SUM(reply_count) AS total_replies FROM replies GROUP BY category
),
ranked AS (
  SELECT r.category, r.username, r.reply_count,
         ROW_NUMBER() OVER (PARTITION BY r.category ORDER BY r.reply_count DESC, r.username) AS rk,
         t.total_replies
  FROM replies r JOIN totals t USING (category)
)
SELECT category,
       username AS top_responder,
       reply_count,
       total_replies,
       ROUND(100.0 * reply_count / total_replies, 1) AS share_pct,
       CASE WHEN 100.0 * reply_count / total_replies >= 30 THEN 'CONCENTRATED' ELSE '' END AS flag
FROM ranked
WHERE rk = 1
ORDER BY share_pct DESC, category
"
```

### Probe 3a — Built-in unanswered list

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny unanswered
```

### Probe 3b — Unanswered topics by views (deterministic SQL form)

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "
SELECT id, title, category, posts_count, views, created_at::DATE AS created
FROM topic_summary
WHERE posts_count <= 1 AND closed = false AND archived = false
ORDER BY views DESC, posts_count DESC, id
"
```

### Probe 4 — Top users by activity

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "
SELECT username,
       total_posts,
       topics_participated,
       topics_created,
       replies_written,
       topics_helped,
       first_seen::DATE AS first_seen,
       last_seen::DATE AS last_seen
FROM user_activity
ORDER BY total_posts DESC, username
LIMIT 15
"
```

### Probe 5 — Topic velocity per month

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "
SELECT date_trunc('month', created_at)::DATE AS month,
       COUNT(*) AS topics,
       SUM(posts_count) AS posts,
       SUM(views) AS views
FROM topic_summary
GROUP BY 1
ORDER BY 1
"
```

---

## Drill-downs (L2 — local mode)

These queries make LLM calls (OpenAI gpt-4.1-mini, `--mode local`). Re-running incurs token cost.

### Topic 20 — Can't get past the Drowned Market (Help & Hints)

```bash
uv run discourse-explorer query sample/fixtures/seed42-tiny \
  "Summarize the unanswered help request in topic 'Can't get past the Drowned Market — any tips?' — what is the user stuck on, what context is given, and what kind of response would unblock them?" \
  --mode local
```

### Topic 21 — Softlock when interacting with the pirate-hopeful protagonist (Bug Reports)

```bash
uv run discourse-explorer query sample/fixtures/seed42-tiny \
  "Summarize the unanswered bug report 'Softlock when interacting with the pirate-hopeful protagonist' — what is the reproduction scenario, what diagnostic information is given, and what's missing that would let a maintainer respond?" \
  --mode local
```

### Topic 23 — Untranslated music stems in Crown of Brine II: The Phantom (Translations & Localization)

```bash
uv run discourse-explorer query sample/fixtures/seed42-tiny \
  "Summarize the unanswered localization gap 'Untranslated music stems in Crown of Brine II: The Phantom' — what is the scope of missing content, what action is the user requesting, and who would need to respond?" \
  --mode local
```

---

## Enriched-references batch

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "
SELECT id, title, category, posts_count, views, like_count, created_at::DATE AS created, tags
FROM topic_summary
WHERE id IN (20, 21, 23)
ORDER BY id
"
```
