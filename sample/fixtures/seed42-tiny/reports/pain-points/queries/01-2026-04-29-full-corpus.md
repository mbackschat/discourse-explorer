# Stats and query commands — Seed42-tiny pain-points audit

**Report:** [`../01-2026-04-29-full-corpus.md`](../01-2026-04-29-full-corpus.md)
**Showboat:** [`01-2026-04-29-full-corpus-showboat.md`](01-2026-04-29-full-corpus-showboat.md)
**Data dir:** `sample/fixtures/seed42-tiny`
**Generated:** 2026-04-29
**Scope:** full corpus (33 topics, 116 posts)

---

## Schema verification

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "DESCRIBE topic_summary"
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "DESCRIBE posts"
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "DESCRIBE topic_threads"
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "DESCRIBE user_activity"
```

---

## Skeleton probes

### Probe 1 — Theme frequency (corpus-calibrated keyword sweep)

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "
WITH themes(theme) AS (VALUES
  ('softlock'),('crash'),('glitch'),('bug'),('broken'),('stuck'),('workaround'),
  ('not working'),('doesn''t work'),('can''t'),('cannot'),('unable'),
  ('untranslated'),('mistranslat'),('localization'),
  ('issue'),('problem'),('help'),('confused'),('unclear'),
  ('remaster'),('amiga'),('android')
)
SELECT t.theme,
       COUNT(DISTINCT p.topic_id) AS topics,
       COUNT(*) AS posts
FROM themes t
JOIN posts p ON p.plain_text ILIKE '%' || t.theme || '%'
GROUP BY t.theme
ORDER BY topics DESC, posts DESC, theme
"
```

### Probe 2 — Top-engagement unresolved

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "
SELECT id, title, category, posts_count, views, created_at::DATE AS created
FROM topic_summary
WHERE posts_count >= 5 AND closed = false AND archived = false
ORDER BY posts_count DESC, views DESC, id
LIMIT 20
"
```

### Probe 3 — Category × theme crosstab

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "
WITH themes(theme) AS (VALUES ('softlock'),('crash'),('glitch'),('workaround'),('untranslat'),('remaster'),('amiga')),
     hits AS (
  SELECT p.category, t.theme, COUNT(DISTINCT p.topic_id) AS topics
  FROM themes t JOIN posts p ON p.plain_text ILIKE '%' || t.theme || '%'
  GROUP BY p.category, t.theme
)
SELECT category, theme, topics
FROM hits
WHERE topics > 0
ORDER BY category, topics DESC, theme
"
```

### Probe 4 — Gap-admission phrasing

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "
SELECT topic_id, post_number, username, LEFT(plain_text, 180) AS snippet
FROM posts
WHERE plain_text ILIKE '%not supported%'
   OR plain_text ILIKE '%not possible%'
   OR plain_text ILIKE '%can''t do that%'
   OR plain_text ILIKE '%no way to%'
   OR plain_text ILIKE '%won''t work%'
   OR plain_text ILIKE '%no plan%'
   OR plain_text ILIKE '%short answer is no%'
   OR plain_text ILIKE '%doesn''t exist%'
ORDER BY topic_id, post_number
"
```

---

## Drill-downs (L2, --mode local)

```bash
uv run discourse-explorer query sample/fixtures/seed42-tiny \
  "Summarize the gameplay help thread 'Hint needed: the lighthouse cipher' — what's the puzzle, what's the user's blocker, what hints did the community offer?" \
  --mode local

uv run discourse-explorer query sample/fixtures/seed42-tiny \
  "Summarize the bug report 'Doubloon SDK fails to load music stems' — what's the failure mode, what diagnostics are given, is there a workaround?" \
  --mode local

uv run discourse-explorer query sample/fixtures/seed42-tiny \
  "Summarize the discussion 'Glitchless category for Crown of Brine' — what speedrun rules are being debated, what edge cases came up?" \
  --mode local

uv run discourse-explorer query sample/fixtures/seed42-tiny \
  "Summarize the localization issue 'Untranslated dialogue trees in Crown of Brine: Reborn' — what's missing, which languages, who is requesting it?" \
  --mode local

uv run discourse-explorer query sample/fixtures/seed42-tiny \
  "Summarize 'Localization notes for Chapter 3: Below the Brine' — what translation gaps or quality issues are flagged, what consensus is forming?" \
  --mode local

uv run discourse-explorer query sample/fixtures/seed42-tiny \
  "Summarize 'Softlock when interacting with the lighthouse hermit' — what's the reproduction path, who has confirmed it, is there a workaround? (cross-user confirmation pattern)" \
  --mode local
```

---

## Enriched-references batch

```bash
uv run discourse-explorer stats --path sample/fixtures/seed42-tiny sql "
SELECT id, title, category, posts_count, views, like_count, created_at::DATE AS created, tags
FROM topic_summary
WHERE id IN (15, 16, 20, 21, 23, 26, 27, 31, 34)
ORDER BY id
"
```
